# docs/sessions/

One HTML changelog per Claude Code session, generated by `node scripts/generateSessionChangelog.js <SessionID>`. See CLAUDE.md → Logging, Reporting & Progress Tracking → Per-session change report.

**Known gap:** `scripts/generateSessionChangelog.js` does not exist yet in this repo. Until it's built, per-session HTML reports can't be generated automatically. Logged in `PROGRESS.md` under session CC-20260730-x4qz.
